# Microsoft Store Release

Openza Reader is live on the Microsoft Store:

- https://apps.microsoft.com/detail/9NNPMN0JSSW5?hl=en-us&gl=IN

Store distribution avoids self-signed installer friction because Microsoft signs the submitted MSIX package and handles trusted install/update delivery.

## Partner Center Steps

These steps require the project owner's Microsoft Partner Center account:

1. Create or open a Partner Center developer account.
2. Reserve the app name `Openza Reader`.
3. Associate the Visual Studio project with the reserved Store app.
4. Create a Release x64 package using **Publish > Create App Packages** and the **Microsoft Store** option.
5. Upload the generated `.msixupload` package in Partner Center.
6. Complete Store listing, age rating, pricing, privacy, and support fields.
7. Submit for certification.
8. After certification completes, verify the public Store listing and install path.

## Package Notes

- Target: Windows 10 22H2 and Windows 11.
- Minimum package version: `10.0.19045.0`.
- Next planned package version: `1.1.1.0`.
- Architecture: x64 for V1.
- Package type: MSIX upload package generated by Visual Studio.
- Store signing: handled by Microsoft Store after submission.
- Runtime: build and package as self-contained for normal user installs.
- WebView2: the app shows an install/repair prompt if the Runtime is missing or broken.
- Local test signing keys such as `Openza.Reader_TemporaryKey.pfx` must never be committed.
- Visual Studio association with the Store app will update package identity values assigned by Partner Center.

## Store Listing

Short description:

```text
Native WinUI 3 Markdown reader for Windows.
```

Description:

```text
Openza Reader is a native Markdown reader for Windows, built with WinUI 3, WebView2, and MSIX packaging.

Open Markdown files from File Explorer, drag and drop documents into the app, reopen recent files, browse headings with a table of contents, search rendered text, adjust zoom, copy selected content, and choose an installed editor from Windows when you need to make changes.

Openza Reader focuses on clean, safe reading. Switch between Preview, Raw, and Side by side read-only modes, choose a reader theme, and control remote image loading from Settings. Markdown is treated as untrusted input, raw HTML is escaped, scripts are blocked by construction, and external navigation is handled through the system browser.
```

Keywords:

```text
markdown, reader, md, documentation, readme, winui, openza
```

Support URL:

```text
https://github.com/openza/reader/issues
```

Privacy policy URL:

```text
https://github.com/openza/reader/blob/main/PRIVACY.md
```

Website:

```text
https://solanky.dev/openza/reader/
```

Category:

```text
Productivity
```

Price:

```text
Free
```

## Release Smoke Test

- Build Release x64 package.
- Install package locally from Visual Studio.
- Open the app from Start.
- Open `.md` and `.markdown` files from File Explorer.
- Confirm file association.
- Confirm drag and drop.
- Confirm TOC, search button, Preview/Raw/Side by side modes, settings persistence, recents, themes, remote image policy, zoom, copy, reload, and external editor actions.
- Confirm the packaged manifest uses `MinVersion="10.0.19045.0"`.
- Confirm uninstall removes the app cleanly.

## Store Release Notes For 1.1.0

```text
Openza Reader 1.1 improves the reading experience with Preview, Raw Markdown, and Side by side modes; visible toolbar labels; a Recent files screen; reader themes; document stats; focus mode; and polished Settings/About pages.

This release also improves privacy and release readiness with a remote image setting, stricter WebView2 runtime error handling, self-contained .NET build output, and updated public documentation.
```

## Store Release Notes For 1.1.1

```text
Openza Reader 1.1.1 fixes Markdown documents stored in Google Drive failing to render with an ERR_FILE_NOT_FOUND error.

This update also separates the development package identity from the Microsoft Store identity so local development can coexist safely with the installed Store app.
```

## References

- https://learn.microsoft.com/windows/apps/package-and-deploy/publish-first-app
- https://learn.microsoft.com/windows/msix/package/packaging-uwp-apps
- https://learn.microsoft.com/windows/apps/publish/publish-your-app/upload-app-packages
- https://learn.microsoft.com/windows/msix/package/sign-msix-package-guide
